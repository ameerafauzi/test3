<!-- 
  <div class="notify_form notify-form-{{ product.id }}" id="notify-form-{{ product.id }}" {% if product.available %}style="display:none"{% endif %}>
    <p class="message"></p>
    {% form 'contact' %}
      {% if form.posted_successfully? %}
        <p><em>Thanks! We will notify you when this product becomes available!</em></p>
      {% elsif form.errors %}
        <p><em>Please provide a valid email address.</em></p>
      {% else %}
        <p>
          <label for="contact[email]"><strong>Notify me when this product is available:</strong></label>
          
          {% if customer %}
            <input type="hidden" class="notify_email" name="contact[email]" id="contact[email]" value="{{ customer.email }}" />
          {% else %}
            <input required type="email" class="notify_email" name="contact[email]" id="contact[email]" placeholder="Enter your email address..." value="{{ contact.fields.email }}" />
          {% endif %}
          
          <input type="hidden" name="challenge" value="false" />
          <input type="hidden" name="contact[body]" class="notify_form_message" data-body="{{ 'products.notify_form.message_content' | t: product: product.title, url: canonical_url }}" value="{{ 'products.notify_form.message_content' | t: product: product.title, url: canonical_url }}" />
          <input class="button btn-default" type="submit" value="Send" style="margin-bottom:0px" />    
        </p>
      {% endif %}
    {% endform %}
  </div>


 -->



  <div class="notify_form notify-form-{{ product.id }}" id="notify-form-{{ product.id }}" {% if product.available %}style="display:none"{% endif %}>
    <p class="message"></p>
    {% form 'contact' %}
      {% if form.posted_successfully? %}
        <p><em>{{ 'products.notify_form.post_success' | t }}</em></p>
      {% elsif form.errors %}
        <p><em>{{ 'products.notify_form.post_error' | t }}</em></p>
      {% else %}
        <p>
          <label for="contact[email]"><strong>{{ 'products.notify_form.description' | t }}:</strong></label>
          
          {% if customer %}
          	<br>
            <input type="hidden" class="notify_email" name="contact[email]" id="contact[email]" value="{{ customer.email }}" />
          {% else %}
          	<br>
            <input required type="email" class="notify_email" name="contact[email]" id="contact[email]" placeholder="{{ 'products.notify_form.email' | t }}" value="{{ contact.fields.email }}" />
          {% endif %}
          
          <input type="hidden" name="challenge" value="false" />
          <input type="hidden" name="contact[body]" class="notify_form_message" data-body="{{ 'products.notify_form.message_content' | t: product: product.title, url: canonical_url }}" value="{{ 'products.notify_form.message_content' | t: product: product.title, url: canonical_url }}" />
          <input class="button btn-default" type="submit" value="{{ 'products.notify_form.send' | t }}" style="margin-bottom:0px" />    
        </p>
      {% endif %}
    {% endform %}
  </div>
